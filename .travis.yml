language: node_js
node_js:
    - "6"
before_script:
    - pip install --user awscli
    - export PATH=$PATH:$HOME/.local/bin
    - npm install -g gulp
script: npm run build
after_success:
    - scripts/deploy.sh
